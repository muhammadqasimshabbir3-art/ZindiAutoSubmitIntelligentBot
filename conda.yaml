# For more details on the format and content:
# https://github.com/robocorp/rcc/blob/master/docs/recipes.md#what-is-in-condayaml
# Tip: Adding a link to the release notes of the packages helps maintenance and security.

channels:
  # Define conda channels here.
  - conda-forge

dependencies:
  - python=3.10.12
  - pip=25.1.1
  - setuptools>=65.5.1
  - uv=0.7.9
  - pip:
      # NOTE: Do not edit this. If you want to add packages to this
      # DW, add them to `requirements.txt` instead.
      - hatchling
      - robocorp-tasks
      - rpaframework==28.6.3
      - pre-commit>=2.17.0
      - pillow>=10.0.1
      - pandas==2.1.4
      - pydantic-yaml==1.2.0
      - urllib3<=1.26.19
      - selenium-stealth==1.0.6
      - retry==0.9.2
      - requests==2.32.2
      - python-dateutil==2.8.2
      - simplejson==3.19.2
      - python-dotenv
      - google-auth-oauthlib
      - jira>=3.5.0
      - office365-rest-python-client>=2.3.0
      - tqdm
